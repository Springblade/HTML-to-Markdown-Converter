export const siteConfig = {
  name: 'HTML to Markdown Converter',
  description: 'Crawl any website and convert to Markdown using crawl4ai',
  apiUrl: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001',
};
